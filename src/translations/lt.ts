import { TranslationContext } from "./types";
import pagesLt from "./pages/lt";
import sharedLt from "./shared/lt";

const lt: TranslationContext = {
  pages: pagesLt,
  shared: sharedLt,
};

export default lt;
