import { SharedNamespace } from "./types";

const en: SharedNamespace = {
    language: "Language",
    contentLoader: "Loading content",
    uiColapse: "Collapse",
    uiExpand: "Expand",
};

export default en;
